# Model Artifacts Folder

This folder is used by the desktop UI as the hardcoded default location for model bundle files.

Required files:
- model.pt
- scaler.pkl
- feature_columns.json
- thresholds.json
- model_metadata.json
- bank_mapping_rules.yaml

Place your production artifacts in this folder before running the desktop UI.

